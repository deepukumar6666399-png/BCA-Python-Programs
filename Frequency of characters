# Program to count frequency of characters

string = input("Enter a string: ")
freq = {}

for ch in string:
    if ch in freq:
        freq[ch] += 1
    else:
        freq[ch] = 1

print("\nCharacter Frequency:")
for key in freq:
    print(key, ":", freq[key])
